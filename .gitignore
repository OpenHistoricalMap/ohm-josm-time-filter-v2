# Build output
build/
build-test/
# dist/ is left tracked as a directory so the path exists, but its
# contents are excluded by *.jar / *.class below.
*.class
*.jar

# Test dependencies (download via apt install junit5, or fetch from Maven Central into lib/)
lib/

# Test output
test/results.txt

# Large local test fixtures (kept locally for reproducing reported bugs)
test_osm_data.osm
test/test_*.osm

# Stray screenshots / one-off images outside resources/
selection-icon.png
*.png
!resources/**/*.png

# macOS
.DS_Store
._*

# IDE
.idea/
.vscode/
*.iml

# OS
Thumbs.db

# AI
CLAUDE.md

# JOSM source
core/
